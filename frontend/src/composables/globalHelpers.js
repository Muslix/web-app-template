// src/composables/globalHelpers.js
import { computed, ref, reactive } from 'vue'
import { useRouter, useRoute } from 'vue-router'

export const globalHelpers = {
  computed,
  ref,
  reactive,
  useRouter,
  useRoute,
}
